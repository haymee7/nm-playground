<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.zzimcar.dao.BlogDao">
  <insert id="save" parameterType="BlogDto">
    INSERT playground.blog (
      title, post, writer
    ) VALUES (
      #{title}, #{post}, #{writer}
    )
  </insert>

  <select id="retrieveOne" parameterType="int" resultType="BlogDto">
    SELECT
      b.pid, b.title, b.post, b.writer, b.pid, b.created_at
    FROM
      playground.blog b
    WHERE
      b.pid = #{pid}
  </select>

  <select id="retrieveByCnt" parameterType="int" resultType="BlogDto">
    SELECT
      b.title, b.post, b.writer, b.pid, b.created_at
    FROM
      playground.blog b
    LIMIT
      #{sp}, #{cnt}
  </select>

  <select id="totalCnt" resultType="int">
    SELECT
      count(*)
    FROM
      playground.blog
  </select>
</mapper>